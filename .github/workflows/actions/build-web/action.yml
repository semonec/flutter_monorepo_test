name: Build Web
runs:
  using: "composite"
  steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Pub get
      working-directory: ./app2
      run: flutter pub get
    - name: Build
      if: ${{ github.ref == 'refs/heads/main' }}
      working-directory: ./app2
      run: flutter build web
